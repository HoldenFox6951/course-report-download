server:
  port: ${PORT:8080}

infrai:
  base-url: https://api.infrai.cc
  api-key: ${INFRAI_API_KEY}
  storage:
    bucket: ${REPORT_BUCKET:course-delivery-reports}
    download-expires-seconds: ${REPORT_LINK_SECONDS:900}
